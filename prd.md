# Openclaw 用户使用数据看板

一个openclaw的用户数据看板，按日、周、月三个维度统计用户在openclaw中对话次数、消耗的token数、调用的skill和对应的次数.

## openclaw用户接入方式

openclaw用户通过安装一个安装包、skill或其他适合的方式

## 触发统计

当openclaw记录日志的文件出现变动时进行统计，如果是第一次接入，将历史数据进行完整收集，后续按照每次日志的变动触发统计更新

## 数据看板

1. 使用vue或其他最适合的框架，做一个轻量化的前端

2. 提供简单的交互，支持切换日、周、月

3. 看板统计的每个维度都是一个按用量排序的列表，支持倒序和顺序


** Note: **

scripts目录中是一份简单的统计脚本，统计token数、对话次数、调用的skill等，其统计的skill并不符合要求，看结果是一些tool的调用，例如read、exec、web_search等，而目标是统计用户安装或用户目录下的skill使用情况